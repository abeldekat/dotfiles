#!/bin/bash

format_clock(){
	echo "%{F$1}%{B$2} ${line} %{B-}%{F-}"
}

while read -r line ; do 
	printf "%s\n" "S$(format_clock $COLOR_SYS_FG $COLOR_SYS_BG)"
done \
	< <(clock -sf ' %a %H:%M') > "$PANEL_FIFO" &

