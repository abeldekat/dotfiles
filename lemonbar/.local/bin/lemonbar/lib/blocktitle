#!/bin/bash

format_xtitle(){
	echo "%{F$1}%{B$2} ${line} %{B-}%{F-}"
}

while read -r line ; do 
	printf "%s\n" "T$(format_xtitle $COLOR_TITLE_FG $COLOR_TITLE_BG)"
done \
	< <(xtitle -sf '%s\n') > "$PANEL_FIFO" &

