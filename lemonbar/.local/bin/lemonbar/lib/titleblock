#!/bin/bash

. panel_colors

mytitle(){
	local fg=$COLOR_SYS_FG
	local bg=$COLOR_SYS_BG
	echo "%{F$fg}%{B$bg} ${line} %{B-}%{F-}"
}

while read -r line ; do 
	echo "T$(mytitle)"
done < <(xtitle -sf '%s\n')

