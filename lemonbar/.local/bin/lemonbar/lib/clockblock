#!/bin/bash

. panel_colors

myclock(){
	local fg=$COLOR_SYS_FG
	local bg=$COLOR_SYS_BG
	echo "%{F$fg}%{B$bg} $ICON_CLOCK ${line} %{B-}%{F-}"
}

while read -r line ; do 
	echo "C$(myclock)"
done < <(clock -sf '%a %H:%M')

