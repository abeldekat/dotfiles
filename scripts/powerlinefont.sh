#!/bin/sh


# mv 'SomeFont for Powerline.otf' ~/.local/share/fonts/
# Je kunt een font met curl installeren:

# Move the patched font to a valid X font path. Valid font paths can be listed with xset q
# fc-cache -vf ~/.local/share/fonts/
# mkdir -p ~/.local/share/fonts
# cd ~/.local/share/fonts && curl -fLo "Droid Sans Mono for Powerline Nerd Font Complete.otf" https://github.com/ryanoasis/nerd-fonts/raw/master/patched-fonts/DroidSansMono/complete/Droid%20Sans%20Mono%20Nerd%20Font%20Complete.otf
# fc-list | grep "Hack" 

# Voor nu heb ik de ttf vanuit github gedownload. Na install is er een ttf in de local fonts map
