# Add a visual layer on top of sxhkdrc in which direct keys manipulate the node 
# Most important operations: Expand, contract, move, window state 
# Read only, digits and letters are disabled for normal typing. 
# All super keys still work

# vim like change config to base mode, empties file sxhkdrc_modes_placeholder
########
# MODE #
########
Escape
    . $XDG_CONFIG_HOME/sxhkd/normal.mode

# set the window state
{t,shift + space,space,f}
 	bspc node -t '~{tiled,pseudo_tiled,floating,fullscreen}'

# expand node
{h,j,k,l}
	bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}

#contract node
shift + {h,j,k,l}
	bspc node -z {right -20 0,top 0 20,bottom 0 -20,left 20 0}

# move node
ctrl + {h,j,k,l}
	bspc node -v {-20 0,0 20,0 -20,20 0}

# ----
#  Safety
#  ----

# make an effort to disable any other typing
{0-9,a-z,A-Z}
	echo "dummy" > /dev/null
